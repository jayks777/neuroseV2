import { Link } from "react-router-dom";

export default function NotFound() {
  return (<>
    
    <div class="flex items-center justify-center h-screen">
      <h1 class=" font-boldc title">Não encontrado [404]</h1>
    </div>

    </>);
}